import { Box, Typography } from "@mui/material";

const Contact = () => (
  <>
    <Box sx={{ minHeight: "100vh", width: "100%", border: "1px solid purple" }}>
      <Typography variant={"h4"} color="text.primary">
        Contact Page
      </Typography>
    </Box>
  </>
);

export default Contact;
